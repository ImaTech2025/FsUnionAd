# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Use system JDK 17
org.gradle.java.home=/Library/Java/JavaVirtualMachines/jdk-17.jdk/Contents/Home
# Kotlin daemon JVM
kotlin.daemon.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Use in-process Kotlin compilation to avoid daemon + sandbox + Unicode path issues
kotlin.compiler.execution.strategy=in-process

# ===== UnionAd SDK 版本号 =====
# VERSION_NAME: 语义化版本号(带后缀标识实现语言,例如 1.0.0-java / 1.1.0-java)
# VERSION_CODE: 整型自增号,用于 maven 制品区分;每次发版 +1
# CI 可通过 -PVERSION_NAME=... -PVERSION_CODE=... 覆盖
VERSION_NAME=1.0.0-java
VERSION_CODE=1

# ===== Maven 制品坐标 =====
# GROUP_ID: Maven groupId,默认 com.ima.union;若公司整体有 group,可在此改
GROUP_ID=com.ima.union

# ===== GitHub Packages 配置 =====
# 发布目标仓库的所有者/仓库名,影响 maven URL: https://maven.pkg.github.com/<owner>/<repo>
# 可通过 -Pgithub.owner=... -Pgithub.repo=... 覆盖
github.owner=ima-global
github.repo=FsUnionAd